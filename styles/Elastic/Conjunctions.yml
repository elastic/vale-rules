extends: substitution
message: "Use '%s' instead of '%s'. The article depends on pronunciation, not spelling."
level: suggestion
ignorecase: true
action:
  name: replace
swap:
  'a HTML': an HTML
  'a HTTP': an HTTP
  'a HTTPS': an HTTPS
  'a SQL': an SQL
  'a SSH': an SSH
  'a SSL': an SSL
  'a XML': an XML
  'a API': an API
  'a IDE': an IDE
  'a URL': an URL
  'a URI': an URI
  'a RSS': an RSS
  'a SSD': an SSD
  'a SVG': an SVG
  'a XSS': an XSS
  'an GUI': a GUI
  'an PDF': a PDF
  'an USB': a USB
  'an CPU': a CPU
  'an GPU': a GPU
  'an RAM': a RAM
  'an SDK': a SDK
  'an FAQ': a FAQ
  'an CSV': a CSV
  'an JSON': a JSON