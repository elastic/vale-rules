extends: substitution
message: "Use device-agnostic language when possible. Use '%s' instead of '%s'."
level: suggestion
ignorecase: true
action:
  name: replace
swap:
  'click': select
  'hit': select
  'long press': select and hold
  'pinch': zoom out
  'press': select
  'spread': zoom in
  'tap': select