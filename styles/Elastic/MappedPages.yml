extends: existence
message: "Only technical writers should add or update mapped_pages in frontmatter."
level: warning
nonword: true
scope: raw
tokens:
  - '(?ms)\A---\r?\n.*?^[ \t]*mapped_pages:.*?^---'
